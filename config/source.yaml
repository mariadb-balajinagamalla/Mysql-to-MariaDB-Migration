# Source config for assessment (read-only)
client:
  mysql_bin: /opt/homebrew/opt/mysql@8.4/bin/mysql 
  host: 127.0.0.1
  port: 13306
  user: root

# Prefer passwords via environment variables in CI:
env:
  MYSQL_PWD: "Root@1234"

target:
  type: mariadb
  version: "11.8.x-enterprise"
